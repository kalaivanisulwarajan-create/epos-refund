# ── HubSpot ─────────────────────────────────────────────────────────────────
# Create a Private App in HubSpot → Settings → Integrations → Private Apps
# Required scopes: crm.objects.deals.read, crm.objects.deals.write,
#                  files, crm.objects.notes.write
HUBSPOT_TOKEN=pat-na1-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ── Gmail (Google Workspace) ─────────────────────────────────────────────────
# 1. Enable 2-Step Verification on the sender account
# 2. Go to Google Account → Security → App Passwords → generate one
# 3. Use that 16-char password below (no spaces)
GMAIL_SENDER=noreply@epos.com.sg
GMAIL_APP_PASSWORD=xxxxxxxxxxxxxxxx

# ── People ───────────────────────────────────────────────────────────────────
DIRECTOR_EMAIL=kalaivani.sulwarajan@epos.com.sg
DIRECTOR_NAME=Sales Director
ADMIN_EMAIL=admin@epos.com.sg
FINANCE_EMAIL=finance@epos.com.sg

# ── URLs (update for production) ─────────────────────────────────────────────
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:8000
